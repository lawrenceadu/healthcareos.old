{
  "name": "healthcare-os",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "apps/healthcare-os",
  "projectType": "application",
  "targets": {
    "build": {
      "executor": "@nrwl/next:build",
      "outputs": ["{options.outputPath}"],
      "defaultConfiguration": "production",
      "options": {
        "root": "apps/healthcare-os",
        "outputPath": "dist/apps/healthcare-os"
      },
      "configurations": {
        "development": {
          "outputPath": "apps/healthcare-os"
        },
        "production": {}
      }
    },
    "serve": {
      "executor": "@nrwl/next:server",
      "defaultConfiguration": "development",
      "options": {
        "buildTarget": "healthcare-os:build",
        "dev": true
      },
      "configurations": {
        "development": {
          "buildTarget": "healthcare-os:build:development",
          "dev": true
        },
        "production": {
          "buildTarget": "healthcare-os:build:production",
          "dev": false
        }
      }
    },
    "export": {
      "executor": "@nrwl/next:export",
      "options": {
        "buildTarget": "healthcare-os:build:production"
      }
    },
    "test": {
      "executor": "@nrwl/jest:jest",
      "outputs": ["{workspaceRoot}/coverage/{projectRoot}"],
      "options": {
        "jestConfig": "apps/healthcare-os/jest.config.ts",
        "passWithNoTests": true
      },
      "configurations": {
        "ci": {
          "ci": true,
          "codeCoverage": true
        }
      }
    },
    "lint": {
      "executor": "@nrwl/linter:eslint",
      "outputs": ["{options.outputFile}"],
      "options": {
        "lintFilePatterns": ["apps/healthcare-os/**/*.{ts,tsx,js,jsx}"]
      }
    }
  },
  "tags": []
}
