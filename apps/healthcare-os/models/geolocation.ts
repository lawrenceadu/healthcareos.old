export interface CountryModel {
  id: string;
  code: string;
  name: string;
}

export interface RegionModel {
  id: string;
  name: string;
}

export interface DistrictModel {
  id: string;
  name: string;
}
